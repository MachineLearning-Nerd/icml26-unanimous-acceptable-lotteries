# Evaluation

The fixed command regenerates all 40 seed runs and summaries, executes the independent checker, and reruns Claims 1, 3, 4, and 5. Every scientific mismatch exits nonzero. Finite seeded means are corroboration; the sampling/drift/Jensen certificate is required for `VERIFIED`.
