# Claim 3 evaluation checkpoint

Status before the HF run: **BLOCKED (results pending)**.

The fixed project command executes the historical regression suite, the Claim 3 scaling verifier, its independent checker, and both negative controls. The run must print raw CSV, checker JSON, control JSON, Git SHA, actual CPU allocation, and runtime. Any failed obligation raises an assertion and exits nonzero.
