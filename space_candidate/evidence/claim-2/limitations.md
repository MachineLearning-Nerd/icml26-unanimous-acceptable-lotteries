# Limitations and deviations

The seeded family has a controlled coordinate basis rather than arbitrary stakeholder geometry. It probes the source sample size through `m=32` (`r=15,376`) and 32,768 agents. Universal expected complexity relies on the independently reconstructed proof certificate, not fitted slopes.

The 200-round cap is only a fail-closed implementation guard and is not used as evidence or selected from the claimed formula.
